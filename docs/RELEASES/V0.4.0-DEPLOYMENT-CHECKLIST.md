# GrowthOS v0.4.0 Deployment Checklist

## Pre-Merge

- Confirm branch is `feature/research-hub-core`.
- Confirm branch head is `b4c29977350a417ef9bd7eaca1f6f26c1540964b` or a later documentation-only handoff commit.
- Confirm the PR targets `main`.
- Confirm PR title is `feat(research): implement Research Hub core`.
- Confirm PR checks, if configured, are passing.
- Confirm the PR contains no forbidden artifacts:
  - `.env` files.
  - Secrets.
  - Virtual environments.
  - `node_modules`.
  - `.next`.
  - Database files.
  - Logs.
  - Generated reports.
  - Encoding backups.
  - Unrelated changes.
  - Copied third-party source code.

## Required Local Validation Evidence

- Backend tests: 36 passed.
- Frontend lint: passed.
- Frontend production build: passed.
- Playwright smoke: passed.
- Live Ollama research: passed.
- Mock fallback: passed.
- Brand Brain integration: passed.
- AI Content Studio regression: passed.
- Source integrity: passed.

## Environment Prerequisites

- Python virtual environment under `apps/api/.venv`.
- Backend dependencies installed from `apps/api/requirements.txt`.
- Node dependencies installed under `apps/web/node_modules`.
- Ollama installed and reachable at `http://localhost:11434` for live local AI validation.
- A supported Ollama model installed. The validated model was `llama3.2:latest`.

## Backend Startup

```powershell
cd apps\api
.\.venv\Scripts\python.exe -m uvicorn main:app --host 127.0.0.1 --port 8000
```

Or use the project script:

```powershell
.\scripts\run-api.ps1
```

## Frontend Startup

```powershell
cd apps\web
npm run dev
```

Or use the project script:

```powershell
.\scripts\run-web.ps1
```

## Recommended Local AI Configuration

```powershell
$env:AI_PROVIDER = "ollama"
$env:AI_FALLBACK_PROVIDER = "mock"
$env:OLLAMA_BASE_URL = "http://localhost:11434"
$env:OLLAMA_MODEL = "llama3.2:latest"
```

## Smoke Checks

- Open `http://localhost:3000`.
- Confirm Research Hub renders.
- Create a research run.
- Confirm provider badge.
- Confirm synthesis-only source notice.
- Confirm copy works.
- Confirm regenerate works.
- Confirm delete works.
- Confirm history updates.
- Confirm Brand Brain context can be selected.
- Confirm AI Content Studio still generates, regenerates, and copies content.
- Confirm mobile navigation includes Research.
- Stop or misconfigure Ollama and confirm mock fallback behavior.

## Post-Merge

- Pull latest `main`.
- Confirm `main` matches `origin/main`.
- Confirm `git status` is clean.
- Create GitHub Release `v0.4.0` titled `GrowthOS v0.4.0 - Research Hub`.
- Update roadmap after merge:
  - Research Hub: Complete.
  - Publishing Engine: Next.
  - Website Intelligence: Planned.
  - Lead Intelligence Integration: Planned.
  - Workflow Automation: Planned.
  - Authentication and workspace ownership: Planned.
  - Database migration: Planned.

## Rollback

- Revert the squash merge commit from `main`.
- Remove any local `apps/api/data/research-runs.json` test data if needed.
- Restart backend and frontend from the previous stable tag.
